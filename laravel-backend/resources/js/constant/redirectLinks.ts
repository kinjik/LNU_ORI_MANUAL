interface RedirectUrl {
  [key: string]: string;
}
export const redirectLink: RedirectUrl = {
  faculty: "/faculty-dashboard",
  admin: "/admin-dashboard",
  "research-coordinator": "/coordinator-dashboard",
};
